pipeline {
    agent { 
        label 'agent' 
    }
    environment {
        PIP_BREAK_SYSTEM_PACKAGES=1
    }
    stages {
        stage('Checkout SCM') {
            steps {
                checkout scm
            }
        }
    }
    
}